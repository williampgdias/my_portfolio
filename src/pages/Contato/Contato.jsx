import React from 'react';

import Form from '../../components/Bootstrap/Form/Form';

const Contato = () => {
    return (
        <div>
            <Form />
        </div>
    );
};

export default Contato;
